name: Container Integration Tests

on:
  push:
    branches:
      - main
      - dev
  pull_request:
    branches:
      - main
      - dev
  workflow_dispatch:

jobs:
  test-compose-deployment:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e .[full,prefect]

      - name: Build images locally
        run: |
          docker compose -f .github/compose/docker-compose.test.yml build

      - name: Start services
        run: |
          docker compose -f .github/compose/docker-compose.test.yml up -d postgres
          
      - name: Start Prefect server on host
        run: |
          prefect server start &
          echo "Waiting for Prefect server to be ready..."
          timeout 60 bash -c 'until curl -sf http://localhost:4200/api/health > /dev/null 2>&1; do sleep 2; done'
          echo "✅ Prefect server is ready"

      - name: Test server health
        run: |
          curl -f http://localhost:4200/api/health

      - name: Start worker
        run: |
          docker compose -f .github/compose/docker-compose.test.yml up -d worker

      - name: Check worker logs
        run: |
          sleep 10
          docker compose -f .github/compose/docker-compose.test.yml logs worker

      - name: Verify worker connection
        run: |
          # Check that worker connected successfully
          docker compose -f .github/compose/docker-compose.test.yml logs worker | grep -i "Starting Prefect worker" || true
          docker compose -f .github/compose/docker-compose.test.yml logs worker | grep -i "worker" || true

      - name: Test CLI commands
        run: |
          # Test basic CLI commands
          python -m w2t_bkin.cli --help
          python -m w2t_bkin.cli version
          python -m w2t_bkin.cli --version

      - name: View all logs
        if: always()
        run: |
          docker compose -f .github/compose/docker-compose.test.yml logs

      - name: Cleanup
        if: always()
        run: |
          docker compose -f .github/compose/docker-compose.test.yml down -v

  test-runtime-detection:
    runs-on: ${{ matrix.os }}
    
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e .[full,prefect]

      - name: Test CLI basic commands
        run: |
          python -m w2t_bkin.cli --help
          python -m w2t_bkin.cli version

      - name: Test CLI data commands
        run: |
          python -m w2t_bkin.cli data --help

  test-podman-deployment:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install Podman
        run: |
          sudo apt-get update
          sudo apt-get install -y podman podman-compose

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e .[full,prefect]

      - name: Build images with Podman
        run: |
          podman build -f docker/Dockerfile -t w2t-bkin:worker .

      - name: Test CLI in Podman container
        run: |
          # Test that CLI works inside container
          podman run --rm w2t-bkin:worker python -m w2t_bkin.cli --help

      - name: Test worker container CLI
        run: |
          # Test worker CLI
          podman run --rm w2t-bkin:worker python -m w2t_bkin.cli version
          # Test ffmpeg
          podman run --rm w2t-bkin:worker ffmpeg -version
